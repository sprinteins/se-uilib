{
    "name": "app-html",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "private": true,
    "scripts": {
        "start": "http-server -p 8080 -c-1 ./public",
        "build:lib": "cd ../uilib && yarn && yarn build && cd ../app-html",
        "copy-lib": "mkdir -p ./public/lib && cp -R ../uilib/dist/* ./public/lib/",
        "build": "yarn build:lib && yarn copy-lib"
    },
    "devDependencies": {
        "http-server": "^0.12.3"
    },
    "dependencies": {
        "dpdhl-ui-lib": "file:../ui-lib"
    }
}