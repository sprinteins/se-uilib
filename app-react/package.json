{
  "name": "app-react",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "serve": "vite preview",
    "build": "yarn build:lib && yarn build:react",
    "build:react": "tsc && vite build",
    "build:lib": "yarn lib:link && yarn link dpdhl-uilib",
    "lib:link": "cd ../uilib && yarn && yarn build && yarn link && cd ../app-react",
    "postinstall": "yarn lib:link"
  },
  "dependencies": {
    "react": "^17.0.0",
    "react-dom": "^17.0.0"
  },
  "devDependencies": {
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "@vitejs/plugin-react-refresh": "^1.3.1",
    "typescript": "^4.1.2",
    "vite": "^2.3.0"
  }
}